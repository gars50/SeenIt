import requests

def import_data():
    print("Running import_data")
    response = "Data OK"
    return response






#Get all requests from Ombi
#For each request, check if the request already exists. If not, create a request, associate the user with the email address